{
  "name": "letta-herdr-mod",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Letta Code mod that reports semantic agent state to Herdr.",
  "scripts": {
    "check": "bun test && bun run typecheck && bun run build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --target=node --format=esm --outfile=dist/letta-herdr-mod.mjs",
    "install:local": "bun run build && mkdir -p ~/.letta/mods && cp dist/letta-herdr-mod.mjs ~/.letta/mods/letta-herdr-mod.mjs"
  },
  "devDependencies": {
    "@types/node": "latest",
    "bun-types": "^1.3.14",
    "typescript": "latest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/klittle32/letta-herdr-mod.git"
  },
  "bugs": {
    "url": "https://github.com/klittle32/letta-herdr-mod/issues"
  },
  "homepage": "https://github.com/klittle32/letta-herdr-mod#readme"
}
